<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Log In: URL Shortener by Aditya</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Mono:ital,wght@0,100..700;1,100..700&display=swap" rel="stylesheet">
    <link rel="icon" type="image/png" href="https://cdn-icons-png.flaticon.com/128/1822/1822940.png">
    <style>
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        /* Apply fade-in animation to all elements */
        * {
            animation: fadeIn 0.6s ease-in-out;
        }

        body {
            font-family: 'Courier New', Courier, monospace;
            background-color: rgb(10, 10, 10);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            /* background-image: url('https://c0.wallpaperflare.com/preview/729/634/463/abstract-backdrop-background-bright.jpg');
            background-repeat: no-repeat;
            background-size: cover;
            background-position: center; */
        }

        nav {
            width: 100%;
            background-color: rgba(10, 10, 10, 0.5); /* Semi-transparent black */
            padding: 2px 0;
            position: fixed;
            top: 0;
            left: 0;
            z-index: 999; /* Ensure the navbar is above other content */
            text-align: center;
        }

        .container {
            margin-top: 60px; /* Adjust margin to accommodate the navbar */
            background-color: rgba(143, 141, 141, 0.327); /* translucent white */
            backdrop-filter: blur(12px); /* apply blur effect */
            border-radius: 30px;
            padding: 40px;
            box-shadow: -1px 0.5px 4px 0px rgb(255, 255, 255);
            text-align: center;
            transition: transform 0.5s ease; /* Apply transition for smooth animation */
            transform-style: preserve-3d;
            transform: rotateY(20deg) rotateX(20deg);
            max-width: 65%;
        }

        .container:hover{
            transform: rotateY(0deg) rotateX(0deg);
        }

        h1 {
            margin-bottom: 30px;
            color: rgb(243, 243, 243);
            font-family: 'Roboto Mono';
            font-size: medium;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label {
            margin-bottom: 8px;
            font-weight: bold;
            color: #555;
        }

        input {
            padding: 10px;
            margin-bottom: 20px;
            width: 60%;
            border: 1px solid #262626;
            border-radius: 40px;
            box-sizing: border-box;
            background-color: #555;
            transition: all 0.35s ease 0s;
            color: whitesmoke;
        }

        input:focus{
            border-radius: 20px;
            width: 69%;
        }

        button {
            padding: 12px 24px;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            background-color: #006de1;
        }

        button:hover {
            background-color: #007ffe;
            box-shadow: 0 0 1.5em #007bff;
            color: #ffffff;
        }

        p.error-message {
            color: rgb(255, 23, 23);
            margin-top: 20px;
        }

        button.sign-up-button{
            border: 1px solid greenyellow;
            border-radius: 4px;
            background-color: transparent;
            width: 107px;
            padding: 4px;
            margin-top: 0;
        }

        button.sign-up-button:hover{
            background-color: greenyellow;
            color: rgba(255, 113, 255, 0.945);
            box-shadow: 0 0 1em greenyellow;
        }
        
    </style>
</head>
<body>

    <nav>
        <img style="margin-top: 3px;" src="https://github.com/adityatakharya/Personal-Portfolio/blob/main/assets/images/logo.png?raw=true" width="140" alt="Logo">

    </nav>

    <div class="container">
        <h1>Please, log in to access the website :)</h1>
        <form method="POST" action="/user/login">
            <input type="email" id="email" name="email" placeholder="Email" required>
            <input type="password" id="password" name="password" placeholder="Password" required>
            <button class="log-in-button" type="submit">Log In</button>
        </form>
        <% if(locals.notfound){ %>
            <p class="error-message">Incorrect username or password! Try again.</p>
        <% } %>
        <h3 style="color: whitesmoke; margin-bottom: 10px; margin-top: 50px" >New here?</h3>
        <button class="sign-up-button" onclick="redirectToSignUp()">Sign Up</button>
    </div>

    <script>
        function redirectToSignUp(){
            window.location.href = "/user/signup";
        }
    </script>
</body>
</html>
